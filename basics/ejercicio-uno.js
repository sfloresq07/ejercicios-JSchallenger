// Write a function that takes two values, say a and b, as arguments
// Return true if the two values are equal and of the same type

const comparison = (a, b) => {
  if (a === b) {
    return true;
  }
  return false
};

console.log(comparison(4,2));

/* const comparison = (a,b) => {
    return (a === b)
}
console.log(comparison(5,2))  */
